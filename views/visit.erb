<h2>Записаться</h2>
<p>На этой странице вы сможете записаться к нам в Barber Shop</p>

<script type="text/javascript">

  $(function() {
  
    $('#color').simplecolorpicker();
    $('#datetimepicker').datetimepicker();
  
  })

</script>


<form action="/visit" method="POST">
  
  <div class="form-group">
    <label>Ваше имя</label>
    <input name="client_name" type="text" class="form-control" placeholder="Введите ваше имя" value="<%= @client_name %>">
  </div>

  <div class="form-group">
    <label>Телефон</label>
    <input name="client_phone" type="text" class="form-control" placeholder="Введите номер вашего телефона" value="<%= @client_phone %>">
  </div>

  
  <div class="form-group">
    <label>Когда вас ждать?</label>
    <input id="datetimepicker" name="client_date" type="text" class="form-control" placeholder="Выберите дату и время вашего визита" value="<%= @client_date %>">
  </div>

  <div class="form-group">  
    <label>Выберите парикмахера</label>
    <select name="barber" class="form-control">
      <% db = get_db %>
      <% db.execute 'SELECT * FROM Barbers' do |row| %>
        <option <%= @barber == row['barber'] ? 'selected' : '' %>><%= "#{row['barber']}" %></option>
      <% end %>
      </select>
  </div>

  <div class="form-group">
    <label>Цвет краски: </label>
    <select id="color" name="color">
      <option <%= @color == '#7bd148' ? 'selected' : '' %> value="#7bd148">Green</option>
      <option <%= @color == '#5484ed' ? 'selected' : '' %> value="#5484ed">Bold blue</option>
      <option <%= @color == '#a4bdfc' ? 'selected' : '' %> value="#a4bdfc">Blue</option>
      <option <%= @color == '#46d6db' ? 'selected' : '' %> value="#46d6db">Turquoise</option>
      <option <%= @color == '#7ae7bf' ? 'selected' : '' %> value="#7ae7bf">Light green</option>
      <option <%= @color == '#51b749' ? 'selected' : '' %> value="#51b749">Bold green</option>
      <option <%= @color == '#fbd75b' ? 'selected' : '' %> value="#fbd75b">Yellow</option>
      <option <%= @color == '#ffb878' ? 'selected' : '' %> value="#ffb878">Orange</option>
      <option <%= @color == '#ff887c' ? 'selected' : '' %> value="#ff887c">Red</option>
      <option <%= @color == '#dc2127' ? 'selected' : '' %> value="#dc2127">Bold red</option>
      <option <%= @color == '#dbadff' ? 'selected' : '' %> value="#dbadff">Purple</option>
      <option <%= @color == '#e1e1e1' ? 'selected' : '' %> value="#e1e1e1">Gray</option>
    </select>
  </div>

  <button type="submit" class="btn btn-primary">Записаться</button>

</form>